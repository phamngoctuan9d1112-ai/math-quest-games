type Props = {
    streak: number;
    onLeaderboard: () => void;
    onLogout: () => void;
};

export default function RightPanel({
    streak,
    onLeaderboard,
    onLogout,
}: Props) {
    return (
        <div>
            {/* code */}
        </div>
    );
}